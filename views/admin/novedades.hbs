    <div class="contenedor-mayor">
    <h4>Noticias actuales:</h4>
    </div>
    <div class="contenedor-novedades">

        {{#each novedades}}
        <div>
            <div class="novedades">
                <div class="contenido-novedad">
                    <h5>{{titulo}}</h5>
                    <p>{{subtitulo}}</p>
                    <button class="btn" data-toggle="modal" data-target="#Modal{{id}}">Más información</button>

                </div>
            </div>
        </div>
        {{/each}}
    </div>
    {{#each novedades}}
    <div class="modal fade" id="Modal{{id}}" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="ExampleModalLabel">{{titulo}}</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <p>{{contenido}}</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>
    {{/each}}